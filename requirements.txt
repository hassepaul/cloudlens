# CloudLens API — pinned runtime dependencies
# Update with: pip-compile requirements.in

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
uvloop==0.21.0
h11==0.14.0

# Pydantic / settings
pydantic==2.10.4
pydantic-settings==2.7.0

# Azure SDK — async clients
azure-cosmos==4.9.0
azure-storage-blob==12.24.0
azure-servicebus==7.14.0
azure-keyvault-secrets==4.9.0
azure-identity==1.19.0

# HTTP client
httpx==0.28.1

# Resilience
tenacity==9.0.0

# Forecasting (Holt-Winters) — lightweight, no training infra
numpy==2.1.3

# Structured logging
structlog==24.4.0

# PDF generation
reportlab==4.2.5

# Auth — Azure AD JWT validation (JWKS)
python-jose[cryptography]==3.3.0

# Observability
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
azure-monitor-opentelemetry==1.6.4
